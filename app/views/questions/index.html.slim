-if user_signed_in?
        -@questions.each do |question|
                p= link_to 'show', question_path(question.id)
                p= question.title
                p= question.body
                p= question.user_id
                hr

-else 
        -@questions.each do |question|
                p= question.title
                p= question.body
                hr
